# 05 实施路线与任务拆解

> 状态：设计方案，未实现  
> 基线：e4b6af7  
> 当前事实 = 静态代码证据；指标 = 拟定验收目标  
> 本文档不执行任何改造，仅为规划参考

---

## 阶段总览

```
Phase 1 (P0, 1-2 周)  ─ 数据安全基线
Phase 2 (P1, 2-4 周)  ─ 性能与搜索
Phase 3 (P1, 3-5 周)  ─ 产品能力补全
Phase 4 (P2, 4-6 周)  ─ 架构治理
Phase 5 (P2, 持续)    ─ 质量与生态
```

**资源假设**：1 名全栈开发（熟悉 Rust + React），兼职投入 ~60%。  
**角色标注**：R = Rust 后端，F = 前端 TS/React，D = 设计/产品，Q = 测试。

---

## Phase 1：数据安全基线（P0）

| 任务 | 改造文件 | 前置 | 角色 | 说明 |
|------|----------|------|------|------|
| T1-01 原子写模块 | 规划新增 `src-tauri/src/atomic_write.rs` | 无 | R | temp+fsync+rename，Windows 兼容 |
| T1-02 替换所有写入点 | `src-tauri/src/commands.rs` L191,277,473,550,950 | T1-01 | R | write_file/write_text_file/save_note/create_backup/export_note |
| T1-03 错误上报 | `src-tauri/src/commands.rs` L191-194 | 无 | R | save_note 返回 Result，移除 .ok() |
| T1-04 前端保存失败处理 | `src/App.tsx` L998-1051, L1064-1068 | T1-03 | F | 不更新 isDirty、toast 错误、WAL 兜底 |
| T1-05 WAL 机制 | 规划新增 `src/hooks/useAutoSave.ts` | T1-04 | F | localStorage WAL + 启动恢复 |
| T1-06 自动保存触发备份 | `src/App.tsx` L1076-1089 (handleSaveSplit) | T1-02 | F | 分屏也调用 create_backup；去重间隔 ≥5min |
| T1-07 restore_backup 路径校验 | `src-tauri/src/commands.rs` L1009-1014 | 无 | R | 添加 validate_path 调用 |
| T1-08 恢复前备份当前 | `src/components/VersionHistory.tsx` L45-56 | T1-02 | F | handleRestore 前先 create_backup |
| T1-09 删除 Agent 死代码 | `src/agent/` 全目录 | 无 | F | 移除 SQL Agent 复制品 |
| T1-10 移除 delete_file 注册 | `src-tauri/src/lib.rs` L51 | 无 | R | 或重命名为 _deprecated 并加 confirm |
| T1-11 save_image 路径校验 | `src-tauri/src/commands.rs` L374-407 | 无 | R | sanitize note_id |

**兼容与回滚**：
- T1-01/02：原子写为纯内部实现变更，IPC 接口不变，前端无感知
- T1-03：save_note 返回值从 `()` 变为 `Result<(), String>`，前端需适配（但当前前端未调用 save_note，仅用 write_file）
- T1-09：删除 agent 目录后需确认无其他模块 import（当前仅 agent/index.ts 自引用）
- 回滚：revert 对应 commit 即可，无数据迁移

---

## Phase 2：性能与搜索（P1）

| 任务 | 改造文件 | 前置 | 角色 | 说明 |
|------|----------|------|------|------|
| T2-01 SQLite FTS5 索引 | 规划新增 `src-tauri/src/index.rs` | 无 | R | rusqlite + fts5，建表/upsert/delete/search |
| T2-02 索引 commands | `src-tauri/src/commands.rs`, `lib.rs` | T2-01 | R | search_notes/rebuild_index/index_status |
| T2-03 Cargo.toml 加依赖 | `src-tauri/Cargo.toml` | T2-01 | R | rusqlite = { features = ["bundled"] } |
| T2-04 前端搜索适配 | `src/components/Sidebar.tsx` L273-288 | T2-02 | F | 调用新 search_notes，debounce 300ms，分页 |
| T2-05 搜索结果高亮 | `src/components/Sidebar.tsx` | T2-04 | F | snippet 中 `<mark>` 渲染 |
| T2-06 知识图谱增量 | `src/App.tsx` L437-482, L844-847 | 无 | F | 保存后仅更新单节点，不全量刷新 |
| T2-07 Rust 侧 note-updated 事件 | `src-tauri/src/commands.rs` write_file 后 | T2-06 | R | emit 解析后的 summary |
| T2-08 文件监听替代轮询 | 规划新增 `src-tauri/src/watcher.rs` | 无 | R | notify crate + Tauri event |
| T2-09 前端 watcher 适配 | `src/App.tsx` L544-568 | T2-08 | F | 移除 setInterval，listen file-changed |
| T2-10 文件树虚拟化 | `src/components/Sidebar.tsx` L290-321 | 无 | F | react-window 或自实现 |
| T2-11 大文件流式加载 | `src-tauri/src/commands.rs` read_file_binary | 无 | R | >5MB 改用 asset:// 协议 |
| T2-12 tauri.conf asset 协议 | `src-tauri/tauri.conf.json` | T2-11 | R | 配置 assetProtocol scope |

**兼容与回滚**：
- T2-01~03：新增模块，不修改现有逻辑；搜索 command 名变更需前端同步
- T2-08：notify crate 在 macOS 使用 FSEvents，Linux 使用 inotify；需处理事件风暴（debounce 500ms）
- 回滚：保留旧 `search_in_files` 作为 fallback，索引不可用时降级

---

## Phase 3：产品能力补全（P1）

| 任务 | 改造文件 | 前置 | 角色 | 说明 |
|------|----------|------|------|------|
| T3-01 YAML frontmatter 解析 | 规划新增 `src/lib/frontmatter.ts` | 无 | F | gray-matter 或自实现 |
| T3-02 编辑器 frontmatter 渲染 | `src/components/Editor.tsx` | T3-01 | F | 顶部元数据卡片（可折叠） |
| T3-03 标签从 frontmatter 读取 | `src-tauri/src/commands.rs` extract_tags | T3-01 | R | 优先 YAML tags 字段 |
| T3-04 WikiLink 自动补全 | 规划新增 `src/lib/WikiLinkSuggestion.ts` | 无 | F | @tiptap/suggestion 弹窗 |
| T3-05 文件树拖拽移动 | `src/components/Sidebar.tsx` | 无 | F/D | HTML5 drag + rename_file |
| T3-06 保存状态指示器 | `src/components/StatusBar.tsx` | T1-04 | F | idle/dirty/saving/saved/error |
| T3-07 冲突解决 UI | 规划新增 `src/components/ConflictDialog.tsx` | T2-09 | F/D | 三选：本地/磁盘/合并 |
| T3-08 HTML 导出增强 | `src/lib/electronAPI.ts` L58-79 | 无 | F | 使用 Tiptap getHTML() |
| T3-09 PDF 导出修复 | `src/lib/electronAPI.ts` L199-209 | 无 | F/R | Tauri webview print 或 Rust 侧 |
| T3-10 导出脱敏 | 规划新增 `src/lib/sanitize.ts` | T3-08 | F | 正则替换敏感信息 |
| T3-11 AI 流式输出 | `src-tauri/src/commands.rs` ai_chat | 无 | R/F | SSE → Tauri event 逐 token 推送 |
| T3-12 附件路径迁移 | `src-tauri/src/commands.rs` rename_file | 无 | R | 重命名时同步更新 .md 中引用 |
| T3-13 新建即落盘 | `src/App.tsx` handleNewNote | 无 | F | 创建空文件到 currentDir |

**兼容与回滚**：
- T3-01：frontmatter 为增量功能，旧笔记无 frontmatter 时 fallback 到现有逻辑
- T3-04：WikiLink 补全为纯前端增强，不影响已有链接解析
- T3-11：流式 AI 需保留非流式 fallback（部分提供商不支持 SSE）

---

## Phase 4：架构治理（P2）

| 任务 | 改造文件 | 前置 | 角色 | 说明 |
|------|----------|------|------|------|
| T4-01 状态管理拆分 | 规划新增 `src/stores/` | 无 | F | Zustand: useNotesStore/useUIStore/useSearchStore |
| T4-02 App.tsx 瘦身 | `src/App.tsx` | T4-01 | F | 目标 < 300 行（仅布局+路由） |
| T4-03 useAutoSave hook | 规划新增 `src/hooks/useAutoSave.ts` | T1-05 | F | 保存状态机+WAL+错误处理 |
| T4-04 useFileWatcher hook | 规划新增 `src/hooks/useFileWatcher.ts` | T2-09 | F | 封装 Tauri event 监听 |
| T4-05 useKeyboard hook | 规划新增 `src/hooks/useKeyboard.ts` | 无 | F | 快捷键注册集中管理 |
| T4-06 IPC 类型安全 | 规划新增 `src/lib/ipc.ts` | 无 | F | 泛型 invoke + 命名参数 |
| T4-07 electronAPI 重构 | `src/lib/electronAPI.ts` | T4-06 | F | 消除位置参数、统一错误处理 |
| T4-08 CSP 收紧 | `src-tauri/tauri.conf.json` | 无 | R | 移除 unsafe-eval，限制 connect-src |
| T4-09 capabilities 收敛 | `src-tauri/capabilities/default.json` | T4-08 | R | 移除 fs:default，收窄 shell |
| T4-10 validate_path 收窄 | `src-tauri/src/commands.rs` L8-23 | 无 | R | 限制为当前笔记目录 + ~/.z-note |
| T4-11 Vite 分包优化 | `vite.config.ts` | 无 | F | manualChunks: mermaid/katex/pdfjs |
| T4-12 编辑器缓存 LRU | `src/components/Editor.tsx` L174 | 无 | F | 上限 30 + LRU 淘汰 |

**兼容与回滚**：
- T4-01/02：大规模重构，需逐步迁移（先提取 store，再瘦身 App）
- T4-08/09：CSP 收紧可能导致 Mermaid（使用 eval）失败 → 需验证后逐步推进
- 回滚：每个任务独立 commit，可单独 revert

---

## Phase 5：质量与生态（P2，持续）

| 任务 | 改造文件 | 前置 | 角色 | 说明 |
|------|----------|------|------|------|
| T5-01 Rust 单元测试 | 规划新增 `src-tauri/src/tests/` | T1-01 | R/Q | atomic_write/validate_path/extract_* |
| T5-02 前端单元测试 | 规划新增 `src/__tests__/` | T4-01 | F/Q | vitest + testing-library |
| T5-03 集成测试 | 规划新增 `tests/` | T2-02 | Q | Tauri driver 或 WebDriverIO |
| T5-04 CI 门禁 | `.github/workflows/build-release.yml` | T5-01 | Q | typecheck + test + clippy |
| T5-05 性能基准 | 规划新增 `benchmarks/` | T2-01 | Q | 搜索/索引/大文件加载 |
| T5-06 i18n 完善 | `src/lib/i18n.tsx` | 无 | F/D | 中英文完整覆盖 |
| T5-07 插件系统预研 | 规划新增 `docs/plugin-design.md` | T4-01 | F/D | 仅设计文档，不实现 |
| T5-08 故障注入测试 | 规划新增 `tests/fault/` | T1-01 | Q | 模拟断电/磁盘满/权限拒绝 |

---

## 依赖关系图

```
T1-01 ──→ T1-02 ──→ T1-04 ──→ T1-05 ──→ T4-03
  │                      │
  └──→ T1-06            T1-06
  │
  └──→ T5-01

T2-01 ──→ T2-02 ──→ T2-04 ──→ T2-05
  │
  └──→ T2-03

T2-08 ──→ T2-09 ──→ T3-07 ──→ T4-04

T3-01 ──→ T3-02
  │
  └──→ T3-03

T4-01 ──→ T4-02 ──→ T5-02
  │
  └──→ T4-06 ──→ T4-07

T1-09 (独立，无前置)
T1-07 (独立)
T1-08 (依赖 T1-02)
T4-08 ──→ T4-09
```

---

## 风险与缓解

| 风险 | 概率 | 影响 | 缓解 |
|------|------|------|------|
| 原子写在 Windows 上行为差异 | 中 | 高 | T1-01 中增加 Windows CI 测试 |
| SQLite FTS5 bundled 编译慢 | 中 | 低 | 使用 rusqlite bundled feature，CI 缓存 |
| CSP 收紧导致 Mermaid 失败 | 高 | 中 | T4-08 前验证 Mermaid 是否依赖 eval |
| App.tsx 重构引入回归 | 高 | 高 | T4-02 前必须有 T5-02 测试覆盖 |
| notify 事件风暴 | 中 | 中 | T2-08 中 debounce 500ms + 批量处理 |
| 用户已有笔记无 frontmatter | 确定 | 低 | T3-01 设计为可选增强，不强制迁移 |
